# Adventure Publisher

Adventure Publisher generates real HTML skill pages from structured JSON data.

## Why it exists

Version 1 published each PDF page as a full-page image. That worked for the booklet, but it produced web pages that were hard to search, edit, read on a phone, and use with assistive technology.

Version 2 treats each skill as a native web lesson:

- real HTML text
- responsive sections and cards